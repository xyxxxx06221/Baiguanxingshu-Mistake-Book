# 行测错题本｜产品需求文档（PRD）

> 版本：v1.0（开发基线稿）  
> 日期：2026-08-29  
> 产品形态：仅在个人 Mac 本地运行的 Web 应用  
> 核心目标：通过“采集—重做—诊断—复盘—再练—验证”的闭环，持续提高行测正确率，而不只是保存错题。

---

## 1. 产品摘要

这是一个面向单用户的个人行测错题管理与复盘工具。用户上传 PDF、扫描件或手机照片后，系统将题目识别、拆分并转换为可编辑的结构化题目；用户可以像在线题库一样重新作答，补充错因和复盘，查看答案与解析，并按照遗忘风险和错误情况反复练习。

系统按行测板块、题型、知识点、错因、来源和时间记录每一次作答。题目即使已经答对，只要之后再次答错，也会自动回到待复习池。最终的数据看板应回答三个问题：

1. 我主要错在哪里？
2. 为什么会错？
3. 下一次最应该练什么？

产品不以“题量越多越好”为目标，而以“减少重复犯错、提升稳定正确率、缩短正确作答时间”为核心结果。

---

## 2. 背景与用户问题

### 2.1 使用背景

- 用户会使用纸笔完成真实试卷，部分题目无法自动进入现有在线题库的错题本。
- 题目来源可能是完整 PDF、扫描 PDF、单页截图、手机拍照或零散图片。
- 有些题目只有题干，没有标准答案或解析；有些答案需要后续手工导入或修正。
- 用户需要按不同模块观察正确率和错误原因，并处理同一道题二次、三次答错的情况。
- 用户不是软件工程专业人员，因此安装、使用、备份和维护必须简单。

### 2.2 当前痛点

- 纸笔错题难以统一保存、检索和统计。
- 单纯收藏图片无法重做，也不能形成板块和题型数据。
- OCR 可能识别错字、选项、表格或图形；错误内容会直接污染后续答案和统计。
- AI 生成答案不一定等于官方标准答案，若不标明来源，容易产生误导。
- “粗心”太笼统，无法转化为下一次可执行的改进动作。
- 一次重做正确不代表已经掌握，缺少后续复习和再次验证。

---

## 3. 产品原则

1. **复盘优先于收集**：上传题目不是终点；每道错题必须进入可执行的复习流程。
2. **答案有来源、有置信度**：官方答案、用户确认答案和 AI 推测答案必须清楚区分。
3. **原始材料可追溯**：即使转为 Markdown，也要保留题目来源、页码及必要的局部图像，方便核对 OCR。
4. **用户始终可修改**：题干、选项、答案、解析、分类和错因都允许人工纠正，并保留修改记录。
5. **统计基于作答记录**：不覆盖历史结果；每次作答都是独立记录。
6. **本地优先、成本可控**：优先使用本地识别与存储，疑难页再按需调用付费接口。
7. **一次只推进一个动作**：界面应明确告诉用户当前最值得做的事情，减少管理负担。

---

## 4. 成功指标

### 4.1 北极星指标

**经至少两次间隔复习后，在未看答案情况下稳定答对的题目比例。**

仅看首次重做正确率会产生虚假的掌握感，因此“已掌握”至少需要满足：

- 最近两次复习均答对；
- 两次复习之间存在时间间隔（默认至少 3 天）；
- 最近一次未查看答案或提示；
- 若记录作答时长，则用时不显著超过目标用时。

### 4.2 辅助指标

- 各板块首次作答正确率、复习正确率、最近 30 天正确率。
- 各题型重复错误率。
- 同一错因的发生次数和下降趋势。
- 到期题完成率、错题清空率、平均掌握用时。
- 题目录入成功率、OCR 人工修正率、答案确认率。
- 各板块平均作答时间与超时率（用户启用计时后）。

---

## 5. 内容分类体系

### 5.1 一级板块

系统预置以下板块，并允许未来增删：

- 常识判断
- 政治理论
- 言语理解与表达
- 判断推理
- 数量关系
- 资料分析

### 5.2 二级题型（首版建议）

- 常识判断：法律、政治、经济、历史人文、科技、地理、时政、其他。
- 政治理论：马克思主义理论、党史党建、习近平新时代中国特色社会主义思想、政策理论、其他。
- 言语理解与表达：逻辑填空、片段阅读、语句表达、篇章阅读。
- 判断推理：图形推理、定义判断、类比推理、逻辑判断。
- 数量关系：数学运算、数字推理（如适用），并允许自定义细分模型。
- 资料分析：文字型、表格型、图形型、综合型；知识点可含增长率、增长量、比重、平均数、倍数、基期与现期等。

二级题型应允许用户修改，因为不同考试、地区和备考体系的口径可能不同。

### 5.3 标签体系

每道题可拥有多个标签：知识点、解题方法、试卷来源、考试类型、年份、难度、是否真题、是否收藏、是否重点题、OCR 待核对、答案待确认等。

---

## 6. 错因体系

错因采用“主错因 + 可选次错因 + 自由复盘”的结构。系统应主动询问，但不应强迫用户填写冗长文字。

### 6.1 通用错因

1. **知识缺失**：不知道概念、公式、事实或规则。
2. **知识混淆**：记得相关内容，但边界、对象或条件混淆。
3. **审题错误**：漏看否定词、主体、时间、范围、单位或问题要求。
4. **信息提取错误**：材料、图表、题干关系读取错误。
5. **推理链错误**：条件关系、论证方向或中间步骤错误。
6. **方法选择错误**：使用了低效或不适用的方法。
7. **计算错误**：算术、估算、公式代入、单位换算错误。
8. **选项辨析错误**：在相近选项间判断失误或排除不彻底。
9. **时间不足／节奏失控**：会做但超时、仓促作答或未完成。
10. **作答操作错误**：选错、涂错、看串行等。
11. **猜题失误**：无明确依据作答。
12. **题目或答案存疑**：题干缺失、OCR 错误、答案争议或解析不可靠。

### 6.2 将“粗心”转为可改进原因

系统不建议把“粗心”作为最终错因。若用户选择“粗心”，继续追问一个轻量问题：

> 具体是哪一步出了问题？

并给出“漏条件、看错问法、抄错数字、单位错误、计算错误、选项点错、时间紧张、其他”等快捷选项。

### 6.3 板块专属错因示例

- 言语：语境关系判断错误、主旨与细节混淆、过度推断、关联词定位错误。
- 判断：图形规律遗漏、定义要件漏判、充分必要关系错误、论证强弱判断错误。
- 数量：模型识别错误、方程设定错误、边界条件遗漏、计算量过大。
- 资料：基期现期混淆、百分点与百分比混淆、分母选错、估算精度不当、单位量级错误。

系统后续可根据用户复盘文本建议错因，但最终归类由用户确认。

---

## 7. 核心业务闭环

```text
上传 PDF/照片
    ↓
识别与题目切分
    ↓
用户快速校对（题干、选项、图表、答案来源）
    ↓
首次登记（原作答、正确答案、是否错误/收藏）
    ↓
进入重做池
    ↓
独立作答 → 提交后查看结果与解析
    ↓
系统询问错因 → 用户写一句复盘/改进动作
    ↓
按规则安排下次复习
    ↓
连续间隔答对则进入“已掌握”
    ↓
若再次答错，立即回到错题池并提高复习优先级
```

### 7.1 题目状态

- 待校对：OCR 完成，但尚未人工确认。
- 待补答案：题干可用，但没有可信答案。
- 待首次重做：错题已确认，尚未在系统中重做。
- 学习中：正在按复习计划练习。
- 已掌握：满足连续、间隔、无提示答对规则。
- 需复核：题干、图表、答案或解析存在疑问。
- 已归档：暂不参与复习，但保留历史。

状态不是单向永久变化。“已掌握”题再次答错后，自动变为“学习中”，下一复习日默认提前。

### 7.2 默认复习节奏

首版采用容易理解的规则，而非复杂算法：

- 重做答错：次日再练。
- 重做答对但不确定／超时：3 天后再练。
- 重做答对且确定：7 天后再练。
- 连续两次间隔答对：标记已掌握，30 天后抽检。
- 已掌握题抽检答错：回到学习中，次日再练。

用户可修改日期或暂缓。后续版本可依据个人数据升级为自适应间隔算法。

---

## 8. 功能需求

## 8.1 导入中心

### 输入形式

- 拖拽或选择 PDF。
- 上传 JPG、PNG、HEIC 等常见图片。
- 一次上传多张照片，并手工调整顺序。
- 后续可增加“从剪贴板粘贴截图”。

### 导入流程

1. 创建导入任务，显示文件名、页数和处理进度。
2. 判断 PDF 是否有可提取文本；有文本则优先直接提取。
3. 扫描页或照片进行图像预处理：旋转、透视校正、裁边、增强。
4. OCR 识别并自动检测题号、题干、选项、材料、图表和解析区域。
5. 将连续题目切分为单题；共享材料与子题建立关联。
6. 自动建议板块、题型和标签。
7. 进入校对页，低置信度内容突出显示。
8. 用户确认后入库；未确认内容不得直接作为高可信题目参与正式统计。

### 原文件与空间策略

默认采用“结构化内容长期保存，原始大文件短期保存”的策略：

- 长期保存：Markdown/结构化文本、题目必要局部图、来源信息、页码、识别结果。
- 默认不长期保存：完整原始 PDF 和整张高清照片。
- 导入完成且用户确认后，可选择立即删除原文件，或保留 7 天后自动清理。
- 对图表、几何图、复杂公式等无法可靠文本化的内容，只保存裁剪后的必要局部图片并压缩。
- 删除前必须提示：若 OCR 内容之后需要核对，缺少原图可能无法恢复。

> 结论：Markdown 很适合正文、选项和简单表格，但不能可靠替代所有图形。最佳方案是“Markdown + 必要局部图片”，而不是强行把所有视觉内容转成纯文本。

## 8.2 题目校对与编辑

校对页面左右对照：左侧显示来源页或局部图，右侧显示结构化题目。

用户可：

- 修改材料、题干、A—D（或更多）选项。
- 合并或拆分被错误识别的题目。
- 标记共享材料及其子题。
- 调整板块、题型和知识点。
- 输入原先选择、标准答案、答案来源和解析。
- 使用 Markdown 编辑，并预览最终练习效果。
- 标记“稍后复核”。

系统自动检查：题号缺失、选项不完整、答案不在选项范围内、重复题目、疑似乱码等。

## 8.3 题目 Markdown 规范

Markdown 是展示和编辑格式；数据库中的题目字段仍需结构化保存，避免后续统计依赖文本解析。

示例：

```markdown
### 题目

2025 年某地区……（题干）

- A. 选项一
- B. 选项二
- C. 选项三
- D. 选项四

> 来源：某试卷，第 12 页，第 38 题
```

附图使用应用内部资源引用。简单表格可转为 Markdown 表格；复杂统计图、图形推理、公式排版等保留压缩裁图。

用户练习时的“划线”不直接改写原题 Markdown，而保存为独立标注：高亮文本范围、颜色和备注。这样可以清除标注重新做，也不会破坏原题。

## 8.4 答案与解析管理

### 答案来源等级

每道题必须显示以下来源之一：

1. **官方答案**：从原卷答案页或权威来源导入，并经用户确认。
2. **用户确认**：用户手工录入或确认。
3. **AI 生成·已确认**：AI 生成后由用户确认。
4. **AI 推测·未确认**：仅供参考，不计入“标准答案已确认”题集。
5. **暂无答案**：允许先入库，稍后补充。

### 答案处理规则

- AI 不得把自己的推理默认为官方标准答案。
- 用户可以覆盖任何答案，但系统保留历史版本、修改时间和来源。
- 答案被修改后，提示是否重算受影响的历史统计；默认保留当时判定，同时提供按最新答案重算选项。
- 对有争议的题目支持多个答案版本及备注。
- 解析至少包括：正确选项、核心依据；可选包括解题步骤、易错点、更快方法。
- 资料分析、数量关系等应尽可能展示可复核的计算过程。

### AI 生成答案的建议

可以调用外部模型 API，但应采用“按需生成”而非导入时全量生成。推荐流程：先识别题目 → 用户选择需要解析的题 → 模型生成候选答案和解析 → 用户确认。这样成本更低，也减少错误内容污染题库。

## 8.5 重做模式

- 单题卡片显示材料、题干、选项和必要图片。
- A/B/C/D 使用大尺寸选择按钮，支持键盘 1—4 或 A—D 快捷键。
- 提交前不显示答案；可选择“确定／不确定／蒙的”。
- 支持计时：单题计时、整组计时、暂停（可关闭）。
- 支持题干划线、高亮、临时草稿和清除标注。
- 提交后显示：结果、答案来源、解析、上次错因、历史复盘。
- 答错后必须选择主错因；答对但超时或蒙对时也建议复盘。
- 可选择“仍不理解”，将题目加入重点池。
- 支持上一题、下一题、稍后再做和退出后续做。

### 练习入口

- 今日到期。
- 最近错题。
- 二次及以上错题。
- 按板块／题型／错因专项练习。
- 收藏题／重点题。
- 随机抽检已掌握题。
- 自定义组题（题数、时间、板块、难度、来源）。

## 8.6 复盘记录

每次错题复盘包含：

- 本次选择、正确答案、用时和确定程度。
- 主错因、次错因。
- 一句话复盘：“我在哪一步错了？”
- 下一次动作：“以后遇到什么信号，要使用什么方法？”
- 可选笔记、公式或 Markdown 内容。

系统可生成复盘提示，但不应代替用户思考。例如：

- “你当时漏看了哪个限定词？”
- “正确方法和你使用的方法差别在哪里？”
- “下次看到哪种题干特征，应立即想到这个知识点？”

## 8.7 数据看板

### 首页概览

- 今日待复习数。
- 本周新增错题、完成复习数、重新答错数。
- 当前已掌握／学习中／待确认题数。
- 最近 7 天和 30 天复习正确率。
- “今天最值得练”的一个板块或错因。

### 板块分析

对六大板块分别展示：

- 题目数、作答次数、首次正确率、复习正确率。
- 最近 7／30／90 天趋势。
- 二级题型正确率、错误率、平均用时。
- 高频错因。
- 二次错误率和已掌握率。

### 错因分析

- 各错因数量和占比。
- 错因随时间的变化。
- 错因 × 板块交叉分析。
- 可下钻到具体题目和历次作答。

### 统计口径

看板必须允许切换：

- 按题目去重：一题只算一次，适合观察题库结构。
- 按作答次数：每次作答都计算，适合观察练习表现。
- 首次作答与复习作答分开，避免复习题反复出现扭曲总体正确率。
- 题量太少时显示样本数，不用一个看似精确的百分比误导用户。

## 8.8 错题库与搜索

- 按关键词搜索题干、笔记、解析。
- 按板块、题型、错因、状态、来源、日期、答案可信度筛选。
- 列表与卡片视图。
- 批量修改分类、加入练习、归档、导出。
- 重复题检测：相似题干提示合并，避免统计重复。
- 题目详情展示完整时间线：导入、答案修改、每次作答、每次复盘和状态变化。

## 8.9 收藏与价值题

收藏与错题状态彼此独立：

- 收藏：值得以后再看或可代表典型方法。
- 重点：目前仍不理解或重复出错。
- 已掌握：当前复习状态。

一道题可以“已掌握且已收藏”。收藏题可以设置定期抽检，但不会默认与错题混为一谈。

## 8.10 备份、导入与导出

- 一键导出完整备份包：数据库、Markdown、局部图片和设置。
- 支持从备份包恢复，并在恢复前检查版本。
- 支持按筛选结果导出 Markdown／CSV；后续可增加打印版 PDF。
- 应提供自动备份，默认每天一次，仅保留最近若干份。
- 数据库升级必须自动备份，并支持迁移失败回滚。

---

## 9. 页面与信息架构

侧边栏建议保持六个一级入口：

1. **今天**：今日复习任务、快速开始、待处理提醒。
2. **导入**：上传、识别进度、待校对任务。
3. **错题库**：全部题目、筛选、搜索、批量操作。
4. **练习**：到期、专项、收藏、随机抽检、自定义组题。
5. **分析**：总览、板块、题型、错因、趋势。
6. **设置**：识别、AI 接口、数据、备份、分类管理。

### 9.1 视觉设计原则

- 采用符合 macOS／iOS 气质的克制设计：大留白、清晰层级、柔和背景、细边框、适度圆角。
- 使用系统字体，优先适配浅色和深色模式。
- 主要强调色只用于主操作、选中态和关键数据，不做高饱和“考试培训风”。
- 动效短且有意义，例如答题结果、状态切换和任务完成；提供减少动态效果设置。
- 图表优先表达趋势和问题，不堆砌装饰性指标。
- 最重要的按钮始终清楚，例如“开始今日复习”“提交答案”“确认入库”。
- 桌面端优先，最小适配宽度建议 1024 px；暂不投入手机端开发。

---

## 10. 数据模型（开发参考）

### 10.1 核心实体

**Question（题目）**

- id
- material_markdown（共享材料，可空）
- stem_markdown
- options（结构化数组）
- images（局部图资源）
- section、question_type、knowledge_tags
- source_id、source_page、source_question_no
- status、favorite、important
- ocr_confidence、needs_review
- created_at、updated_at

**AnswerVersion（答案版本）**

- id、question_id
- correct_options
- explanation_markdown
- source_type（official/user_confirmed/ai_confirmed/ai_unconfirmed/none）
- source_note、confidence
- created_at、supersedes_id

**Attempt（作答记录）**

- id、question_id、answer_version_id
- selected_options、is_correct
- duration_seconds、certainty（sure/unsure/guess）
- hint_or_answer_viewed
- attempt_context（original/review/mock/manual_record）
- attempted_at

**Review（复盘）**

- id、attempt_id
- primary_error_reason、secondary_error_reasons
- reflection_markdown、next_action
- created_at、updated_at

**ReviewSchedule（复习计划）**

- question_id、due_at、stage
- consecutive_correct
- lapse_count（二次错误次数）
- last_result、mastered_at

**Source（来源）**

- id、name、type（pdf/image/manual）
- exam、year、original_filename
- original_retention_policy、imported_at

**ImportJob（导入任务）**

- id、source_id、status、progress
- recognition_engine、cost_estimate、error_log
- created_at、completed_at

### 10.2 关键约束

- 不删除或覆盖 Attempt；所有趋势依赖完整作答历史。
- Question 与 AnswerVersion 分离，支持答案纠正和争议版本。
- 收藏状态与学习状态分离。
- 共享材料只存一份，多个子题引用，避免重复。
- 用户编辑 OCR 后保留原始识别文本或差异记录，便于问题追踪。

---

## 11. 技术方案建议

### 11.1 推荐形态

首版建议做成“本地 Web 应用”：浏览器访问本机地址，数据和服务均运行在 Mac 上。这样既有 Web UI 的开发便利，也不需要部署服务器。

为降低维护难度，建议：

- 单体应用，不拆分微服务。
- 本地 SQLite 数据库，便于备份和迁移。
- 局部图片使用普通文件目录管理，数据库仅存路径和元数据。
- 提供一个启动脚本，后续可封装成 macOS 应用或菜单栏启动器。
- 配置通过设置页完成，不要求用户编辑代码或配置文件。
- 开源时提供一条命令启动、示例数据、数据库迁移说明和隐私说明。

具体前后端框架在开发前结合现有仓库确定；本 PRD 不锁死技术栈。

### 11.2 识别策略

采用分层识别能兼顾成本和质量：

1. **PDF 原生文字层**：直接提取，成本最低。
2. **Mac 本地 OCR／开源 OCR**：处理清晰扫描件与照片，不产生按次 API 费用。
3. **版面与题目切分规则**：识别题号、选项和共享材料。
4. **疑难页云端视觉模型**：仅对低置信度、复杂表格、公式或图形按需调用。
5. **人工校对**：作为质量最终兜底。

不要把 OCR、题目切分和答案生成视为同一个步骤。三者应分别显示状态、置信度和错误，任何一个失败都可以单独重试。

### 11.3 API 与费用控制

- 支持“完全本地模式”，不配置任何 API 也能完成基础录入和练习。
- 云端服务使用用户自己的 API Key；密钥存入 macOS Keychain，不写入数据库、日志或 Git 仓库。
- 导入前估算页数和可能费用，超过用户设置的单次预算时请求确认。
- 缓存识别与生成结果，相同内容不重复计费。
- 默认只把裁剪后的必要区域发送给云端，减少成本和隐私暴露。
- 设置月度用量上限和使用记录；达到上限后自动回退到本地模式。
- 支持替换 OCR／大模型供应商，避免被单一接口锁定。

### 11.4 隐私与安全

- 默认不监听局域网公网地址，只允许本机访问。
- 原文件删除、批量删除、恢复备份等操作需要明确确认。
- 日志不得记录 API Key；题目内容日志应可关闭或自动清理。
- 上传云端前明确说明将发送哪些页面或裁图。
- Git 默认忽略数据库、用户图片、备份、密钥和日志。

---

## 12. MVP 范围

### 12.1 第一阶段：先形成有效闭环（必须做）

- PDF／图片上传。
- 基础文字识别、单题切分和人工校对。
- 结构化题干、选项、答案和解析编辑。
- 六大板块、题型、标签和通用错因。
- 单选题重做、提交、判定、错因询问和文字复盘。
- 作答历史与“答错回池”状态逻辑。
- 今日复习与简单间隔规则。
- 错题库筛选、搜索、收藏、重点。
- 基础看板：板块／题型／错因／时间趋势。
- SQLite 本地存储、备份和恢复。
- 答案来源与未确认标识。

### 12.2 第二阶段：明显提升体验

- 多题共享材料处理。
- 图表裁剪、复杂表格优化和低置信度标注。
- 题干划线、高亮和草稿。
- 计时与确定度记录。
- AI 题型建议、错因建议、候选答案和解析。
- 重复题检测、批量导入官方答案。
- 自定义组题和打印／Markdown 导出。
- 更完整的数据下钻。

### 12.3 暂缓功能

- 多用户、账号、云同步和权限系统。
- 公网部署、社交、排行榜、课程与题库商城。
- 原生 iPhone／iPad 客户端。
- 自动抓取第三方付费题库。
- 一开始就做复杂的个性化推荐模型。

这些功能会显著增加维护成本，但对首阶段“提升个人正确率”帮助有限。

---

## 13. MVP 验收标准

1. 用户能上传一份 PDF 或一组图片，并看到处理进度及失败原因。
2. 系统能产出可编辑的题干和选项；用户能把错误切分或 OCR 内容改正确。
3. 用户能给题目指定板块、题型、原作答、答案来源及正确答案。
4. 未确认的 AI 答案有明显标识，不能伪装成官方答案。
5. 用户能完成一次单选题重做，提交后看到结果和解析。
6. 答错后能选择错因并写复盘；记录出现在题目时间线中。
7. 重做答错的题会重新进入待复习池，不会因曾经答对而永久消失。
8. 看板能分别统计首次作答和复习作答，并按板块、题型、错因筛选。
9. 用户能找到所有二次及以上答错的题。
10. 用户能收藏题目，且收藏不改变其错题状态。
11. 用户可编辑题目和答案，历史作答不被静默覆盖。
12. 用户能创建备份，并在空白环境中恢复题目、图片、作答和复盘记录。

---

## 14. 关键风险与应对

### 14.1 OCR 并非总能完整转成 Markdown

图形推理、复杂统计图、公式和拍照阴影可能导致识别错误。应对方式是保留局部图、显示置信度和提供快速校对，而不是追求“全自动且纯文本”。

### 14.2 AI 答案可能错误

通过答案来源等级、未确认状态、版本记录和人工确认降低风险。官方答案能导入时应始终优先。

### 14.3 录入流程过重导致弃用

采用“先快速入库、后批量校对”，复盘只要求一个错因和一句话；高级字段均可选。首页优先处理今日任务，不要求用户维护复杂资料库。

### 14.4 统计产生错误结论

首次与复习作答分开，显示样本数，支持按题去重／按次数切换；答案修改后提示统计影响。

### 14.5 本机数据丢失

提供自动备份、版本迁移前备份和可验证恢复。若未来需要跨设备，再考虑加密同步，而不是首版直接引入账号和云服务。

### 14.6 项目难以维护

保持单体、少依赖、数据格式稳定；关键流程配自动测试；设置和迁移尽量图形化；README 面向不会编程的用户编写。

---

## 15. 建议增加的“真正提分”能力

以下能力比单纯增加题库更有价值：

1. **蒙对识别**：答对但选择“蒙的／不确定”，仍进入复习，避免虚假正确率。
2. **耗时正确识别**：答对但严重超时，不直接判定掌握。
3. **重复错题榜**：优先展示二错、三错题，这些通常最值得处理。
4. **可执行复盘**：复盘不仅写“为什么错”，还写“下次看到什么信号就采取什么动作”。
5. **周复盘报告**：每周只给出 1—3 个主要问题、证据和下周训练建议，避免信息过载。
6. **相似错因专项练习**：把不同板块里相同的审题、时间或计算问题串联起来。
7. **掌握抽检**：已掌握题也会低频回访，以真实结果修正掌握状态。
8. **原卷表现补录**：允许快速录入纸笔考试中的原选择和用时，即使当下没有完整解析。

---

## 16. 待开发前确认的产品决策

这些问题不阻塞 PRD，但在正式开发对应功能前需要决定：

- 首批主要处理哪种材料：原生文字 PDF、扫描 PDF，还是手机照片？
- 是否已有一套经常使用的官方答案版式，方便做批量匹配？
- 首版是否必须支持整套试卷自动切题，还是先支持用户框选／逐题确认？
- 是否需要记录原卷作答时间，还是先只记录系统内重做时间？
- 原始 PDF／照片默认立即删除，还是保留 7 天？
- 是否愿意配置云端 API，还是首版必须完全本地可用？
- 看板更关心国考、省考还是统一统计？不同考试是否要分开观察？

---

## 17. 推荐的下一步

1. 选取 2—3 份真实材料作为样本：文字 PDF、扫描件／照片、含图表或共享材料的复杂题。
2. 用样本验证“导入—切题—校对—重做—复盘”流程，先确定交互，不急于接入昂贵 API。
3. 设计 SQLite 数据模型和五个核心页面原型：今天、导入校对、重做、题目详情、分析。
4. 开发最小闭环并实际使用一周，记录哪些字段经常不填、哪些步骤最费时间。
5. 再决定是否加入云端 OCR、AI 答案生成和复杂自适应复习。

---

## 18. 产品结论

这个项目是可行的，并且适合做成本地 Web 应用。PDF 和照片可以转成适合练习的 Markdown／结构化题目，但复杂图表应保留压缩后的局部图片。OCR 和 AI 答案都不应被假设为百分之百正确，因此产品必须把“人工校对”和“答案来源”设计成一等功能。

首版最重要的不是一次性实现所有智能能力，而是可靠地跑通以下闭环：

> 把纸笔错题放进来 → 能重新做 → 明确为什么错 → 安排下一次练习 → 再错就回池 → 用数据证明是否真的减少了同类错误。

只要这个闭环足够顺手并持续保留历史，它就已经能提供现有纸笔流程最缺少的价值。
