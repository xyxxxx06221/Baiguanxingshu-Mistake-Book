# 百官行述（行测助手）阶段性总结与新会话交接

> 生成时间：2026-08-30  
> 用途：汇总早期需求、命名、开发与优化过程，作为公开开发记录。  

## 一、当前结论

项目已从需求梳理推进到可运行的本地 MVP，并确定产品名为 **百官行述**。当前已经具备题目导入、人工筛选入库、错题重做、错因与复盘记录、间隔复习、错题库管理、分板块分析、本地备份以及 macOS 应用打包能力。

现阶段重点不再是搭建基础骨架，而是继续提升真实题卷识别质量、完善答案可信度链路、做稳定性回归，并整理代码结构与版本管理。

## 二、四个会话总结

### 1. 需求文档：确定产品目标与复盘闭环

产出文件：`行测错题本_PRD_v1.0.md`

核心目标不是单纯“存错题”，而是通过持续复盘提高行测正确率。形成的主流程是：

1. 导入 PDF、扫描件或照片；
2. OCR/文档解析得到题干、材料、选项、题型和候选答案；
3. 用户人工确认需要保留的错题及标准答案；
4. 重新作答，记录答案、确定度、错因和一句话复盘；
5. 答错回池，并按复习计划再次出现；
6. 按板块、题型、错因和重复错误形成数据分析。

需求层面的关键决策：

- 单用户、本机优先，不做账户与云端协作；
- 数据默认保存在本机，强调低维护成本；
- 六大板块独立统计：常识判断、政治理论、言语理解与表达、判断推理、数量关系、资料分析；
- AI/OCR 结果不能直接视为标准答案，必须显示答案来源与确认状态；
- 支持收藏、重点题、二次错误回池、题干划线/标注、JSON 备份恢复；
- UI 追求简洁、现代、接近 macOS/iOS 的观感。

### 2. 产品命名与视觉：确定“百官行述”

产品名称最终采用 **百官行述**，定位为兼具公考语境、复盘意味与传统文书气质的个人行测错题工具。

已经落地：

- 应用标题、包名和页面品牌统一为“百官行述”；
- 使用印章风格 Logo，资源位于 `app/public/baiguan-seal.png` 与 `app/public/baiguan-seal-logo.png`；
- macOS 应用标识为 `cn.baiguan.xingshu`；
- 导航和整体视觉经过多轮调整，采用侧栏 + 工作区结构。

该会话后续也承担了一部分页面布局修复：曾尝试固定整屏和右侧独立滚动，但造成底部内容裁切；最终已移除根布局中的整屏高度与 `overflow: hidden` 限制，使长页面能够完整显示。窄侧栏下仅保留图标，悬浮标签显示在内容层上方。

### 3. 按 PRD 开发：完成主体 MVP

主要实现位于 `app/`，技术栈为 React 19、Next/Vinext、TypeScript，本地存储使用浏览器 `localStorage`。

已完成的核心能力：

- 今日复习：首页展示总错题、今日待做、已掌握、未掌握与正确率；
- 答题与复盘：选择答案、确定度、错因、一句话复盘、用时记录；
- 复习调度：答错次日回池，答对按 3/7 天间隔再次验证；
- 错题库：搜索、板块/状态筛选、收藏、重点、编辑、批量加入练习、归档和删除；
- 专项练习：支持单题和共享材料题组；
- 材料与原图：题干和材料可保留视觉引用，支持题干标注；
- 分析看板：六大板块、错因分布、薄弱题型、重复错误和掌握率；
- 导入：PDF/图片上传、识别日志、DeepSeek 视觉结构化；
- 数据安全：题目、作答和复盘保存在本机，可导出/恢复 JSON；
- 桌面化：已配置 Electron，当前存在打包产物 `app/release/mac-arm64/百官行述.app`。

接口相关：

- AI 分析：`app/app/api/analyze/route.ts`
- 设置页允许输入 DeepSeek 凭证，并保存在本机。

### 4. 近期优化：答题反馈、导入边界与导航

本轮针对真实使用体验做了三类关键调整：

1. 答题反馈：正确选项使用淡绿色，错误选择使用红色，清理选中态白边；
2. 导入逻辑：OCR 结果先作为候选题展示，只有用户勾选、补充板块/错因/复盘并确认的题目才进入错题库；
3. 错题库不再承担“待校对队列”，校对工作前移到导入流程。

重要实现边界在 `app/app/import-logic.ts`：选中题目 ID 是入库边界，未选中的候选题不能因调用方疏忽被写入错题库；旧的“待校对”状态在恢复数据时会被清理。

布局方面经历过多轮修正。当前仓库状态以完整长页面为准：

- 根布局不再锁死一屏高度，不再用 `overflow: hidden` 裁切内容；
- 最后一张题卡能够完整位于主容器内；
- 窄侧栏隐藏正文标签、保留图标；
- 导航悬浮名称通过更高层级显示，避免被工作区遮挡。

## 三、当前代码与运行方式

主要文件：

- `行测错题本_PRD_v1.0.md`：产品需求基线；
- `README.md`：简要运行说明；
- `启动行测错题本.command`：本机启动入口；
- `app/app/page.tsx`：当前大部分业务状态和页面组件；
- `app/app/import-logic.ts`：导入候选题的入库边界与恢复清理；
- `app/app/layout.tsx`：全局布局及窄侧栏规则；
- `app/app/*.css`：多轮迭代形成的界面样式；
- `app/electron/main.mjs`、`app/scripts/package-desktop.mjs`：桌面应用入口与打包。

运行方式：

```bash
cd app
pnpm run dev
```

或双击根目录的 `启动行测错题本.command`，浏览器访问 `http://localhost:3000`。

常用检查：

```bash
cd app
pnpm run test
pnpm run typecheck
pnpm run build
pnpm run lint
```

桌面打包：

```bash
pnpm run desktop:pack
```

## 四、已知风险与未完成事项

### P0：新窗口首先确认

- 用一份真实 PDF 和一张手机照片做完整回归：上传 → OCR → 候选题 → 勾选 → 填错因/复盘 → 入库 → 重做 → 分析；
- 检查 DeepSeek 的本机凭证是否仍有效；
- 验证未勾选候选题绝不会进入错题库，旧备份恢复后不存在“待校对”项；
- 在浏览器常用窗口宽度下检查侧栏、悬浮标签、页面底部和长题目是否仍有裁切。

### P1：建议尽快完成

- 为作答调度、备份恢复和主要 UI 流程增加自动化测试；当前已有导入边界与材料处理测试以及统一 `test` 脚本；
- 把体量较大的 `page.tsx` 拆成导入、错题库、练习、分析、设置等独立模块；
- 合并多份叠加 CSS，建立清晰的样式入口，减少后续覆盖和回归问题；
- 增加明确的“标准答案确认/修改”流程和答案来源筛选；
- 对 OCR 失败、接口限额、超大 PDF、空白页和重复导入提供清晰提示；
- 桌面应用需要确认签名、DMG 生成及脱离开发环境后的完整启动行为。

### 工程风险

- 项目根目录当前不是 Git 仓库，无法查看提交历史或安全回滚；继续开发前建议初始化 Git，并先提交当前可运行基线；
- 数据依赖 `localStorage`，清理浏览器数据会丢失内容，应强调定期导出 JSON；
- 2026-08-30 的公开发布前检查中，类型检查、11 个自动化测试与生产构建均通过；ESLint 无错误，有 2 条关于原生 `<img>` 性能的提示。

## 五、建议的新会话开场提示词

```text
请阅读阶段性开发记录、产品需求文档和当前 app 代码。项目名为“百官行述”，是本机运行的行测错题复盘工具。先不要重写现有架构；请先检查当前状态并完成一轮真实导入到复习的端到端回归，列出问题后按 P0 优先级继续开发。必须保持：只有导入时勾选并完成复盘的候选题才能进入错题库；AI 答案不得冒充标准答案；本地数据与已有功能不能丢失。
```

## 六、交接原则

后续开发应始终围绕一个判断标准：功能是否帮助用户发现错因、避免重复犯错并提高真实考试正确率。界面优化、OCR 与 AI 都服务于这条复盘闭环，不应取代人工确认和可靠的数据记录。
